# CLAUDE.md

See @AGENTS.md for coding guidelines and the git/release workflow.

`AGENTS.md` is the single, tool-agnostic source of truth for project rules
(FSO-only file I/O, commit policy, push policy, release management). This file
exists only so Claude Code auto-loads those rules: the `@AGENTS.md` reference
above imports them into context. Do not duplicate guideline content here -- edit
`AGENTS.md` instead.
